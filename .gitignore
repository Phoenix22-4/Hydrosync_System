node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local IDE files and generated Android wrapper bundle
.idea/
android/.idea/
gradle-8.14.3-all.zip

# Firmware folder tracked in Git but excluded from web app builds & hosting
# Keep firmware/ in Git for version control, but don't deploy to web hosting
firmware/
!firmware/
!firmware/**/*.ino
!firmware/**/*.h
