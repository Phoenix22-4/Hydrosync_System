node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local IDE files and generated Android wrapper bundle
.idea/
android/.idea/
gradle-8.14.3-all.zip

# Firmware folder - tracked in Git for version control
# but excluded from web app builds & Netlify hosting via netlify.toml
# Do not add firmware/ here - it needs to be in Git
# Instead, use [build] ignore = "firmware/" in netlify.toml
